name: Release

on:
  push:
    branches:
      - main

concurrency:
  group: release
  cancel-in-progress: false

permissions: {}

jobs:
  tagpr:
    name: Update release pull request or tag release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Ensure tagpr labels exist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh label create tagpr \
            --color 0E8A16 \
            --description "Pull requests managed by tagpr" \
            --force
          gh label create "tagpr:minor" \
            --color FBCA04 \
            --description "Release PR with a minor version bump" \
            --force
          gh label create "tagpr:major" \
            --color B60205 \
            --description "Release PR with a major version bump" \
            --force
      - name: Run tagpr
        uses: Songmu/tagpr@242e22916eb775d6e359959fe3ba5c497de4a01a # v1.20.2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
