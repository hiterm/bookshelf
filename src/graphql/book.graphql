query book($bookId: ID!) {
  book(id: $bookId) {
    id
    title
    authors {
      id
      name
      yomi
    }
    isbn
    read
    owned
    priority
    format
    store
    createdAt
    updatedAt
  }
}
