query bookRevisions($bookId: ID!) {
  bookRevisions(bookId: $bookId) {
    bookId
    revisionNumber
    title
    authorIds
    isbn
    read
    owned
    priority
    format
    store
    purchaseDate
    bookCreatedAt
    bookUpdatedAt
    createdAt
  }
}
