query books {
  books {
    id
    title
    authors {
      id
      name
      yomi
    }
    isbn
    read
    owned
    priority
    format
    store
    purchaseDate
    createdAt
    updatedAt
  }
}
