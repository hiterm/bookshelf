## 1. Align Workflow Triggers

- [x] 1.1 Scope the actionlint push trigger to `main` and add the
  `pull_request` trigger
- [x] 1.2 Scope the zizmor push trigger to `main` and add the
  `pull_request` trigger

## 2. Verify Validation Model

- [x] 2.1 Confirm CI and adjacent release, updater, and Renovate workflows are
  unchanged
- [x] 2.2 Run workflow and repository validation required for the change,
  excluding local frontend integration tests
