## 1. Workflow Implementation

- [x] 1.1 Add `--only-shell` to the Chromium install command in all four Playwright E2E jobs
- [x] 1.2 Verify Playwright configuration and all other workflow steps remain unchanged

## 2. Validation

- [x] 2.1 Run lightweight lint, formatting, typecheck, and workflow validation checks
- [x] 2.2 Verify all four GitHub Actions Playwright jobs pass and inspect browser installation logs
- [x] 2.3 Compare browser install, Playwright test, and total job durations before and after the change

## 3. Completion

- [x] 3.1 Sync the delta spec into the canonical `frontend-ci` specification and archive the change
